# 安伴子女端 Web 骨架

这是 V0.1 的最小静态骨架，先覆盖访问码、设备状态、留言、主动问候、提醒草稿和家庭画像草稿。

## 本地打开

```powershell
cd web
python -m http.server 5173
```

然后访问 `http://localhost:5173`。

## 测试

```powershell
npm test --prefix web
```
