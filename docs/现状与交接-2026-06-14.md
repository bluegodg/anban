# 安伴 现状与交接（真机联调后）— 2026-06-14

> 给接手 anban 代码的人/agent。本文是**真机联调挖出来的现实**；PRD/docs 是理想设计、不含下面这些坑。**动手前先读这份。**

## 已部署并真机验证 ✅
- 服务器 `101.34.214.149`：xiaozhi（manager `:8080` / WS+OTA `:8989`）+ anban（`:8090`）+ 子女网页（`:8091`）。
- 真机已验证：① 老人语音对话；② **子女网页发留言 → 设备开口播报**（完整产品流）。
- 设备 ID：`9c:13:9e:8b:af:28`。AI：豆包 ASR + 豆包(火山方舟)LLM + 豆包 TTS。

## 🔴 别做 / 别动（否则白费力或顶掉已修）
- **保活问题属固件侧**：设备一闲就断 WS、主动播报推不进去——**别在 anban 代码里修**，这是设备/固件层面的事，记录即可。
- **留言不走主动语音配额**（main.go 已不给 message 挂 gate）——**别加回来**。留言点对点、必达。
- **LLM 必须用豆包**：DeepSeek/OpenAI 收不了设备 `self.*` 带点工具名（报 400）。
- **已修，勿重做**：xiaozhiclient 空设备列表 `{"data":null}` 当空处理；cron 锁东八区；vision 默认工具名 `self.camera.take_photo`；greeting/reminder 话术去掉写死人名 + 按时段问候。

## 必须知道的真机事实
- `auth.enable=false` → 设备**不自动注册进 manager 设备表**；而 OpenAPI（inject/status/role）要求"设备在表里且属当前用户"。当前已手工 `INSERT` 一行登记。换新设备同理。
- 设备 MCP 工具（`self.audio_speaker.set_volume`/`self.screen.set_theme`/`self.camera.take_photo`/`self.online_music.play_music`…）是**固件自带**、豆包能直接调用。视觉加分项直接用 `self.camera.take_photo`。
- 部署方式 = **交叉编译二进制 + scp + 重启**（根目录 `deploy.sh`；服务器 `~/anban/start.sh` + `~/anban/anban.env`），**不是** git pull build。**改完代码记得重新部署才会上线生效。**

## 下一步优先级（按 PRD）
1. **人设约束**：profile 的 `BuildPrompt` 里加一句"非老人明确要求，不要更改设备设置/音量/主题/字体"（豆包会自作主张乱调工具）。
2. **PRD #4 完整对话记录**：子女端页面用已有 `xiaozhiclient.GetHistory` 展示对话历史（开发期功能）。
3. 其余 PRD 验收口径打磨。

## 工作纪律
- 架构三铁律不变（见 `AGENTS.md`）；域间不互相 import；只 xiaozhiclient 懂 manager。
- TDD：先写测试再实现。保持 `go test ./...`（server）+ `npm test --prefix web` 全绿。
- 验证：`server/` 下 `GOPROXY=https://goproxy.cn,direct GOSUMDB=off CGO_ENABLED=0 go build/vet/test ./...`。
